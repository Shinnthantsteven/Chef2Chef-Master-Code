# Chef2Chef Master Code

A local-first inventory and stock-management interface.

## Security

This repository is intended to contain application code only. Do not commit passwords, API keys, passport or visa identifiers, transaction references, private company datasets, or other credentials.

The public version uses local file upload and does not require third-party photo-search API credentials.
